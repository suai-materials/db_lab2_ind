<Page x:Class="SuperDBApp.TableView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="TableView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20*" />
            <RowDefinition Height="*" />

        </Grid.RowDefinitions>
        <DataGrid IsReadOnly="True" Name="DataG" AutoGenerateColumns="True"
                  CanUserAddRows="False" AutoGeneratedColumns="DataG_OnAutoGeneratedColumns"
                  AutoGeneratingColumn="TheDataGrid_OnAutoGeneratingColumn" ItemsSource="{Binding Data}">
            <DataGrid.Resources>
            </DataGrid.Resources>
        </DataGrid>
        <Button Click="Delete_OnClick" HorizontalAlignment="Center" Grid.Row="1">Удалить</Button>
    </Grid>
</Page>