<Page x:Class="SuperDBApp.TableView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:SuperDBApp"
      mc:Ignorable="d"
      Title="TableView" >
    <Grid>
        <DataGrid Name="DataG" AutoGenerateColumns="True"
                  CanUserAddRows="False" AutoGeneratedColumns="DataG_OnAutoGeneratedColumns" AutoGeneratingColumn="TheDataGrid_OnAutoGeneratingColumn" ItemsSource="{Binding Data}">
            <DataGrid.Resources>
                <DataTemplate x:Key="DataItemCellTemplate">
                    <ComboBox ItemsSource="{Binding [Option].Options}"
                              SelectedValue="{Binding [Option].SelectedOption, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </DataTemplate>
            </DataGrid.Resources>
        </DataGrid>
    </Grid>
</Page>